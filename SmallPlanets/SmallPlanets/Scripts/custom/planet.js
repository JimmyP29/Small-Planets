


    //var saturn = $('#saturn');
    //var oldSaturnPos = saturn.position().right;
    //var oldWindowWidth = $(window).width();
    //$(window).resize(function () {
    //    var newWindowWidth = $(window).width();

    //    saturn.css({
    //        right: oldSaturnPos - ((oldWindowWidth + newWindowWidth) * 2)
    //    });
    //});


var planet = {
    name: '',
    image: '',
    distanceFromSun: '',
    mass: '',
    diameter: '',
    displayInformation: function (planet) {
        alert('BOOM' + planet);
    }
};